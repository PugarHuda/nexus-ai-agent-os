{
  "name": "nexus-contracts",
  "version": "1.0.0",
  "description": "Nexus smart contracts — Decentralized AI Agent OS on 0G",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network og-mainnet",
    "deploy:testnet": "hardhat run scripts/deploy.js --network og-testnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
